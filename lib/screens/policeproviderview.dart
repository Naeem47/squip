import 'package:flutter/material.dart';

class policeproviderview extends StatelessWidget {
  const policeproviderview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Police provider screen"),),
    );
  }
}