import 'package:flutter/material.dart';

class FIRERIGADE extends StatelessWidget {
  const FIRERIGADE({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("FIREBRIGADE"),
      ),
      body: Column(),
    );
  }
}