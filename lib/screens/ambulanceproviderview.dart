import 'package:flutter/material.dart';

class ambulanceproviderview extends StatelessWidget {
  const ambulanceproviderview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Ambulance provider screen"),),
    );
  }
}