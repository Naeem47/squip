import 'package:flutter/material.dart';

class fireproviderview extends StatelessWidget {
  const fireproviderview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("fire bridge provider screen"),),
    );
  }
}